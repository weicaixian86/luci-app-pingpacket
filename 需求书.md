# luci-app-pingpacket 需求书

## 1. 目的

本文档用于描述 `luci-app-pingpacket` 当前源码对应的功能需求、页面结构、运行逻辑与关键约束，作为后续维护、测试和继续开发的基线文档。

## 2. 适用范围

- OpenWrt 24.10.x
- 已安装 LuCI Web 管理界面
- 设备可执行 `ping`、`awk`、`sed`、`grep`、`tail` 等基础命令

## 3. 菜单结构

LuCI 后台路径：

`状态 -> Ping丢包监控`

子页面：

- `监控`
- `日志`

要求：

- `监控` 排在左侧第一个
- `日志` 排在右侧第二个

## 4. 监控页需求

### 4.1 配置项

系统应支持以下配置：

- 启用/关闭监控
- 国内目标地址
- 国外目标地址

约束：

- 国内、国外目标均支持 IP 或域名
- 两个目标允许只填一个
- 两个目标同时为空时，不允许保存并应用
- 两个目标同时为空时，不允许通过启用开关启用服务

### 4.2 页面展示

监控页应展示以下信息：

- 开始时间
- 运行时长
- 最近更新时间
- CPU 占用
- 内存占用
- 服务状态

服务状态至少包括：

- 已关闭
- 启动中
- 运行中
- 数据停滞

### 4.3 国内/国外监控卡片

每个目标卡片应分别展示：

- 目标名称
- 样本数
- 平均延迟
- 最低延迟
- 最高延迟
- 丢包率

要求：

- 国内和国外独立统计
- 样本数分别显示，不做总和合并
- 长时间运行后样本数可能很大，页面应尽量保证数字完整显示

### 4.4 统计口径

当前统计均为“本次服务启用后的累计统计值”：

- 平均延迟：本次运行期内成功 Ping 的平均 RTT
- 最低延迟：本次运行期内成功 Ping 的最小 RTT
- 最高延迟：本次运行期内成功 Ping 的最大 RTT
- 丢包率：本次运行期内失败次数 / 总样本数
- 样本数：本次运行期内累计探测次数

重置规则：

- 停止服务后重新启用，统计重新开始
- 服务重启后，统计重新开始
- 设备重启后，统计重新开始

### 4.5 页面交互

系统应支持：

- 页面内直接启用/关闭监控
- 点击“保存并应用”保存配置
- 监控已启用时，保存配置后自动重启服务
- 数据停滞时，显示“重新启动服务”按钮

输入保护要求：

- 页面轮询时，不应覆盖用户正在编辑但尚未保存的输入内容
- 输入框失焦后，如当前值与服务端一致，应恢复为非脏状态

## 5. 日志页需求

### 5.1 页面能力

日志页应提供：

- 日志内容展示
- 自动刷新开关
- 清除日志按钮
- 日志最后更新时间
- 日志行数
- 日志大小

交互规则：

- 自动刷新默认勾选
- 取消勾选后停止日志轮询
- 恢复勾选后继续轮询
- 清除日志后页面内容应立即刷新

### 5.2 日志内容

系统应记录以下事件：

- 服务启动
- 服务停止
- 目标丢包
- 目标恢复

当前日志规则：

- 单个目标只要单次 Ping 失败，立即记录一条丢包日志
- 连续失败时，每次失败都单独记录
- 出现失败后，首次恢复响应时记录一条恢复日志

日志约束：

- 日志文件路径：`/tmp/pingpacket.log`
- 最多保留最近 500 行

## 6. 系统资源展示需求

监控页应实时展示：

- CPU 占用百分比
- 内存占用百分比

当前实现规则：

- CPU 占用通过 `/proc/stat` 两次采样差值计算
- 内存占用通过 `/proc/meminfo` 计算
- CPU 首次采样时允许返回 `0.0%`

## 7. 接口需求

### 7.1 `get_data`

返回监控页所需数据，至少包括：

- 启用状态
- 国内目标
- 国外目标
- 服务运行状态
- 开始时间
- 最近更新时间
- CPU 占用
- 内存占用
- 国内统计
- 国外统计

### 7.2 `save_config`

用途：

- 保存启用状态与目标配置

规则：

- 保存前执行“至少配置一个目标”校验
- 启用状态为 `1` 时调用服务重启
- 启用状态为 `0` 时调用服务停止

### 7.3 `restart_service`

用途：

- 前端在数据停滞时手动重启服务

规则：

- 未启用时返回失败
- 两个目标都为空时返回失败

### 7.4 `get_logs`

用途：

- 返回日志内容和日志元信息

### 7.5 `clear_logs`

用途：

- 清空日志文件

## 8. 运行文件约束

当前实现涉及以下主要运行文件：

- 状态文件：`/tmp/pingpacket_status.json`
- 日志文件：`/tmp/pingpacket.log`
- CPU 采样文件：`/tmp/pingpacket_cpu_state`
- 运行目录：`/var/run/pingpacket`
- 启动时间文件：`/var/run/pingpacket/start_time`
- 目标配置缓存：`/var/run/pingpacket/config`

说明：

- 以上均为运行期临时文件
- 停止服务或重启设备后允许丢失

## 9. 安装与升级需求

### 9.1 安装内容

插件安装后应具备以下文件：

- LuCI 控制器
- 监控页模板
- 日志页模板
- init 服务脚本
- 采集脚本
- ACL 配置

### 9.2 配置文件处理

安装脚本应满足：

- 当 `/etc/config/pingpacket` 不存在时自动创建
- 升级时尽量保留已有配置
- 尽量处理历史升级残留的 `/etc/config/pingpacket-opkg`

### 9.3 默认目标

当系统配置项为空时，安装脚本可按需补入默认值：

- 国内：`www.baidu.com`
- 国外：`www.google.com`

## 10. 非功能要求

- 页面应支持桌面端与移动端访问
- 样本数在长时间运行后应尽量完整显示
- 日志文件需具备简单截断能力，避免无限增长
- 页面轮询失败不应导致前端脚本崩溃
- 文档描述应与当前源码行为保持一致

## 11. 验收要点

1. 后台菜单为 `状态 -> Ping丢包监控 -> 监控 / 日志`
2. 两个目标都为空时无法保存或启用
3. 监控页可实时显示 CPU、内存、开始时间、运行时长和最近更新时间
4. 国内/国外统计为本次服务运行期累计值
5. 停止或重启服务后，统计重新开始
6. 日志页可自动刷新、暂停刷新和清除日志
7. 单次 Ping 失败时能立即写入丢包日志
8. 恢复响应后能写入恢复日志
